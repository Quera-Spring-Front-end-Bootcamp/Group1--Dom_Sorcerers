import { extendTheme } from "@chakra-ui/react";

const Theme = extendTheme({
  fonts: {
    heading: "dana",
    body: "dana",
  },
  colors: {
    primary: {
      600: "#208D8E",
    },
  },
});

export default Theme;
